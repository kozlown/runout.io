/**
 * Configs for the whole app
 */

const config = {
    port: 3000,
    database: {
        host: 'localhost',
        user: 'root',
        password: 'root',
        database: 'runout.io'
    }
}

export default config
